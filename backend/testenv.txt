# Database configuration
DATABASE_URL=sqlite:///./data/behaviour.db
BASE_URL=http://localhost:8000

# SMTP configuration email sending
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASSWORD=

# ============ VERTEX AI CONFIGURATION (KEYLESS ADC) ============
# 1. On Cloud Run: Attached Workload SA automatically provides ADC credentials.
# 2. Local Dev: Run `gcloud auth application-default login` with your SSO account.
GOOGLE_CLOUD_PROJECT_ID=hack-team-test2026
VERTEX_AI_LOCATION=us-central1

# Fallback AI Provider (Optional)
OPENROUTER_API_KEY=